-- GENERATED by scripts/extract-migration-dml.mjs — do not edit by hand.
-- One-off admin backfill extracted from 20260824091223_9d6b9c30-c2ce-42a5-a2fb-09fccda2225c.sql.
-- Run it from the admin dashboard (or as service_role) instead of shipping
-- the DML inside a migration.

CREATE OR REPLACE FUNCTION public.admin_backfill_20260824091223_9d6b9c30_c2ce_42a5_a2fb_09fccda22(_dry_run boolean DEFAULT true)
RETURNS jsonb
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public
AS $fn$
DECLARE
  _affected bigint := 0;
  _rows bigint;
BEGIN
  IF NOT public.has_role(auth.uid(), 'admin') THEN
    RAISE EXCEPTION 'admin role required';
  END IF;

  IF _dry_run THEN
    RETURN jsonb_build_object(
      'dry_run', true,
      'source_migration', '20260824091223_9d6b9c30-c2ce-42a5-a2fb-09fccda2225c.sql',
      'statements', 1
    );
  END IF;

  -- from 20260824091223_9d6b9c30-c2ce-42a5-a2fb-09fccda2225c.sql:65
  UPDATE public.realtime_guard_allowlist
  SET sensitivity = 'private',
      note = 'Heartbeat only (profile id + created/updated flag + timestamp). All authenticated users can read because /social surfaces every signed-up user.'
  WHERE table_name = 'profile_pulse';

  GET DIAGNOSTICS _rows = ROW_COUNT;
  _affected := _affected + _rows;

  RETURN jsonb_build_object('dry_run', false, 'rows_affected', _affected);
END;
$fn$;

REVOKE EXECUTE ON FUNCTION public.admin_backfill_20260824091223_9d6b9c30_c2ce_42a5_a2fb_09fccda22(boolean) FROM PUBLIC, anon;
GRANT  EXECUTE ON FUNCTION public.admin_backfill_20260824091223_9d6b9c30_c2ce_42a5_a2fb_09fccda22(boolean) TO authenticated, service_role;
